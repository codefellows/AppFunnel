<h2>Your Application</h2>

<dl class="dl-horizontal">
<% @display_attributes.each do |key, value| %>
  <dt><%= key.capitalize.sub(/[_]/, " ") %></dt>
  <dd><%= value.to_s %></dd>
<% end %>
<% @display_apn_attributes.each do |key, value| %>
  <dt><%= key.capitalize.sub(/[_]/, " ") %></dt>
  <dd><%= value.to_s %></dd>
<% end %>
</dl>

<%= link_to "Edit Profile", edit_profile_path(@profile) %><br>
